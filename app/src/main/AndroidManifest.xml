<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unitglo.airi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Airi">
        <activity android:name=".auth.SignUpActivity"></activity>
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".appintroduction.AppIntroductionMainActivity" />
        <activity android:name=".appintroduction.fragments.AppIntroduction1Fragment" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>